# ── IDE ───────────────────────────────────────────────────────────────────────
.idea/
.vscode/
*.iml
*.iws
*.ipr
out/

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Java / Spring Boot ────────────────────────────────────────────────────────
target/
*.class
*.war
*.ear

# Compiled jars — but keep the Gradle wrapper jar (required to run ./gradlew)
*.jar
!gradle/wrapper/gradle-wrapper.jar

# Gradle build output and daemon cache
**/build/
.gradle/

# Spring Boot local config overrides (never commit credentials or local datasources)
**/application-local.yml
**/application-local.properties
**/application-dev.yml

# ── Logs ─────────────────────────────────────────────────────────────────────
*.log
logs/
